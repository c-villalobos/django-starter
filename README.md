For now, a Django starter that includes the packages:
- `django-tailwind`
- `django-htmx`
- `django-allauth`

Instructions
1. Replace `django-starter`for your project name in `pyproject.toml` and `uv.lock`.
2. `uv sync`